<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is refused. The app carries a bearer token and file bytes on
  every request, and the production backend is HTTPS, so there is nothing to
  relax for.

  The three exceptions are local addresses only: a development backend on the
  machine running the app, or on the LAN. A misconfigured *public* host still
  fails loudly rather than quietly sending tokens in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Loopback, and the alias the Android emulator uses for its host. -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
