# Nimbus Drive backend — runtime dependencies (Python 3.11+)

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12          # multipart/form-data for large-file uploads

# --- Config & validation ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0
greenlet>=3.1                     # required by SQLAlchemy's async bridge

# --- Auth & crypto ---
pyjwt[crypto]>=2.9,<3.0           # RS256 access/refresh tokens
argon2-cffi>=23.1,<26.0           # password hashing
cryptography>=43.0                # AES-256-GCM for bot-token encryption at rest

# --- Telegram ---
httpx>=0.27,<1.0                  # Bot API (small files) + OAuth token exchange
telethon>=1.37,<2.0               # MTProto (large files, streaming)

# --- Infrastructure ---
redis>=5.2,<7.0                   # optional: JWT revocation, rate limits
structlog>=24.4,<26.0
