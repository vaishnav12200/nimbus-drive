-r requirements.txt

pytest>=8.3,<10.0
pytest-asyncio>=0.24,<2.0
pytest-cov>=6.0,<8.0
ruff>=0.8,<1.0
mypy>=1.13,<2.0
asgi-lifespan>=2.1,<3.0

# Note: do NOT add `types-redis`. It is deprecated and its stubs are older than
# the redis package's own inline types, so installing it hides real methods
# (`aclose`) behind spurious mypy errors.
pre-commit>=4.0,<5.0
